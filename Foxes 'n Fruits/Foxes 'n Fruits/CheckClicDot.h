void checkClicDot(Point clicDot, int& clicX, int& clicY) {
	if (clicDot.y >= 1 && clicDot.y <= 5) {
		if (clicDot.x >= 1 && clicDot.x <= 8) {
			clicX = 0;
			clicY = 0;
		}
		if (clicDot.x >= 9 && clicDot.x <= 16) {
			clicX = 1;
			clicY = 0;
		}
		if (clicDot.x >= 17 && clicDot.x <= 24) {
			clicX = 2;
			clicY = 0;
		}
		if (clicDot.x >= 25 && clicDot.x <= 32) {
			clicX = 3;
			clicY = 0;
		}
		if (clicDot.x >= 33 && clicDot.x <= 40) {
			clicX = 4;
			clicY = 0;
		}
		if (clicDot.x >= 41 && clicDot.x <= 48) {
			clicX = 5;
			clicY = 0;
		}
	}
	if (clicDot.y >= 6 && clicDot.y <= 10) {
		if (clicDot.x >= 1 && clicDot.x <= 8) {
			clicX = 0;
			clicY = 1;
		}
		if (clicDot.x >= 9 && clicDot.x <= 16) {
			clicX = 1;
			clicY = 1;
		}
		if (clicDot.x >= 17 && clicDot.x <= 24) {
			clicX = 2;
			clicY = 1;
		}
		if (clicDot.x >= 25 && clicDot.x <= 32) {
			clicX = 3;
			clicY = 1;
		}
		if (clicDot.x >= 33 && clicDot.x <= 40) {
			clicX = 4;
			clicY = 1;
		}
		if (clicDot.x >= 41 && clicDot.x <= 48) {
			clicX = 5;
			clicY = 1;
		}
	}
	if (clicDot.y >= 11 && clicDot.y <= 15) {
		if (clicDot.x >= 1 && clicDot.x <= 8) {
			clicX = 0;
			clicY = 2;
		}
		if (clicDot.x >= 9 && clicDot.x <= 16) {
			clicX = 1;
			clicY = 2;
		}
		if (clicDot.x >= 17 && clicDot.x <= 24) {
			clicX = 2;
			clicY = 2;
		}
		if (clicDot.x >= 25 && clicDot.x <= 32) {
			clicX = 3;
			clicY = 2;
		}
		if (clicDot.x >= 33 && clicDot.x <= 40) {
			clicX = 4;
			clicY = 2;
		}
		if (clicDot.x >= 41 && clicDot.x <= 48) {
			clicX = 5;
			clicY = 2;
		}
	}
	if (clicDot.y >= 16 && clicDot.y <= 20) {
		if (clicDot.x >= 1 && clicDot.x <= 8) {
			clicX = 0;
			clicY = 3;
		}
		if (clicDot.x >= 9 && clicDot.x <= 16) {
			clicX = 1;
			clicY = 3;
		}
		if (clicDot.x >= 17 && clicDot.x <= 24) {
			clicX = 2;
			clicY = 3;
		}
		if (clicDot.x >= 25 && clicDot.x <= 32) {
			clicX = 3;
			clicY = 3;
		}
		if (clicDot.x >= 33 && clicDot.x <= 40) {
			clicX = 4;
			clicY = 3;
		}
		if (clicDot.x >= 41 && clicDot.x <= 48) {
			clicX = 5;
			clicY = 3;
		}
	}
	if (clicDot.y >= 21 && clicDot.y <= 25) {
		if (clicDot.x >= 1 && clicDot.x <= 8) {
			clicX = 0;
			clicY = 4;
		}
		if (clicDot.x >= 9 && clicDot.x <= 16) {
			clicX = 1;
			clicY = 4;
		}
		if (clicDot.x >= 17 && clicDot.x <= 24) {
			clicX = 2;
			clicY = 4;
		}
		if (clicDot.x >= 25 && clicDot.x <= 32) {
			clicX = 3;
			clicY = 4;
		}
		if (clicDot.x >= 33 && clicDot.x <= 40) {
			clicX = 4;
			clicY = 4;
		}
		if (clicDot.x >= 41 && clicDot.x <= 48) {
			clicX = 5;
			clicY = 4;
		}
	}
}
